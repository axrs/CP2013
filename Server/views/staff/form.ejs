<%
var isEditing  = (typeof model != 'undefined' && model != null && typeof model.contId != 'undefined' ) ? true : false;
var buttonLabel = isEditing ? 'Update' : 'Create';
var formTitle = isEditing ? 'Editing : ' + model.contSurname + ',' + model.contForename : 'New Staff Member';
var actionParam = isEditing ? '/staff/' + model.servId : '/staff';
%>

<% include ../header %>
<div class="row-fluid">
    <% include toolbox %>

    <div class="span9">

        <% include ../common/formHeader %>
        <% include ../common/contactForm %>
        <% include ../common/staffForm %>
        <% include ../common/formActions %>
        <% include ../common/formFooter %>

    </div>

</div>
<!--/span-->
<% include ../footer %>